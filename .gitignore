node_modules
dist
dist-ssr
*.local
.vite

# Real secrets (e.g. the WhatsApp Graph API access token) — never commit,
# unlike .env.local.example which documents the shape without real values.
.env
.env.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# Firebase CLI (local deploy cache/logs — never anything secret, but noisy)
.firebase
firebase-debug.log
firebase-debug.*.log

# Wrangler local dev/deploy cache — never anything secret, but noisy
worker/.wrangler
